#!/bin/sh
# Serve the bundled nullclaw-chat-ui static site.
# Connects to the nullclaw WebSocket gateway at ws://127.0.0.1:3000/ws by default.
exec "$SNAP/bin/node" \
  "$SNAP/share/nullclaw-chat-ui/bin/nullclaw-chat-ui.js" \
  run --host 127.0.0.1 --port 4173 "$@"
